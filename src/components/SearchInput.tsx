import { Input } from "@chakra-ui/react";

const SearchInput = () => {
  return (
    <form>
      <Input borderRadius={40} color={"white"}></Input>
    </form>
  );
};

export default SearchInput;
